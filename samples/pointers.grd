
main(){
    x, y := 5
    p, p2 := @x, @y
    r := addPointers(p, p2)
    io.Printf("result: %d", r)
    d, d2 := @p, @p2
    r := addDoublePointers(p, p2)
    io.Printf("result: %d", r)
}

addPointers(@int a, b) int {
    return *a + *b
}

addDoublePointers(@@int a, b) int {
    return *a + *b
}
