
contract Adder {

    x, y int

    constructor(x, y int){
        this.x, this.y = x, y
    }

    add() int {
        r := x + y;
        if r < x && r < y {
            terminate("Addition has overflowed")
        }
    }

    terminate(err string){
        io.Printf("%s", err)
    }
}
